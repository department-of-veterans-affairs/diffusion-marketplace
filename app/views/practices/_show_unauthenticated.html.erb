<section class="usa-section p2-top p2-bottom">
  <div class="grid-container position-relative padding-x-9">
    <div class="grid-row padding-x-9">
      <div id="overview" class="grid-col-12">
        <%= render partial: "shared/messages", locals: {small_text: false} %>

        <div class="bg-white">

          <%# <% if @practice.tagline.present? %>
          <h1 class="font-sans-2xl x0-top margin-bottom-2 font-family-sans text-heavy word-break-break-word hyphens-auto line-height-sans-205">
            <%# <%= @practice.tagline %>
            <%= @practice.name %>
          </h1>
          <div class="margin-bottom-2">
            <a target="_blank" aria-label="Share <%= @practice.name %>" tabindex="-1" aria-hidden="true" href="mailto:?subject=<%= email_practice_subject(@practice) %>&body=<%= email_practice_body(@practice) %>" class="text-no-underline">
              <span class="text-no-underline dm-font-color-blue-50v"><i class="fas fa-external-link-alt padding-right-105"></i>Share link</span>
            </a>
          </div>
          <%# <% end %>
          <p class="font-sans-xl line-height-sans-5"><%= @practice.description %></p>
        </div>
      </div>
    </div>
  </div>
</section>

<section class="usa-section padding-0">
  <div class="grid-container position-relative padding-x-9">
    <div class="grid-row padding-x-9">
      <div class="grid-col-12">
        <div class="grid-row">
          <%#<div class="tablet:grid-col-5 padding-x-1 padding-top-1">
            <% if @practice.main_display_image.present? %>
          <%#= image_tag @practice.main_display_image.url(:thumb), alt: "#{@practice.name} display image" %>
          <%#    <%= image_tag @practice.main_display_image_s3_presigned_url(:thumb), alt: "#{@practice.name} display image" %>
          <%#  <% end %>
          <%#</div>%>
          <div <%#class="tablet:grid-col-7 padding-x-1"%>>
            <p class="practice-summary hyphens-auto word-break-break-word margin-0 font-sans-xs-2 line-height-sans-505"><%= @practice.summary %></p>
          </div>
        </div>
      </div>
      <div class="padding-top-3 margin-bottom-6">
        <%= link_to 'Login to see full practice', new_user_session_path, id: 'practice-page-login-link', class: 'dm-button-link display-inline-block font-sans-xs-2 line-height-sans-505' %>
      </div>
    </div>
  </div>
</section>
<%# <div class="dm-background-triangle-bottom "></div> %>