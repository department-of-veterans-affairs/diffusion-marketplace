<%= form_with(model: area_of_affect, local: true) do |form| %>
  <% if area_of_affect.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(area_of_affect.errors.count, "error") %> prohibited this area_of_affect from being saved:</h2>

      <ul>
      <% area_of_affect.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :short_name %>
    <%= form.text_field :short_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :position %>
    <%= form.number_field :position %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
