<%= form_with(model: job_position, local: true) do |form| %>
  <% if job_position.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(job_position.errors.count, "error") %> prohibited this job_position from being saved:</h2>

      <ul>
      <% job_position.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :short_name %>
    <%= form.text_field :short_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :position %>
    <%= form.number_field :position %>
  </div>

  <div class="field">
    <%= form.label :job_position_category_id %>
    <%= form.text_field :job_position_category_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
