<div id="practiceEditorPublicationModal" class="bg-base-light publication-modal-hidden">
  <div class="padding-y-9 publication-modal">
    <div class="margin-x-auto bg-white radius-md publication-modal-content">
      <div class="text-white radius-top-md">
        <h2 class="margin-0 padding-205 font-sans-lg">Cannot publish yet</h2>
      </div>
      <div class="publication-modal-body font-sans-md">
        <div class="padding-2">
          <p>This is what you need to do before publishing your innovation to the Diffusion Marketplace</p>
        </div>
        <div class="font-sans-md line-height-sans-5 practice-editor-publication-error-container padding-2 border-top-2px border-bottom-2px border-base-lightest">
          <% if @practice %>
            <%# Overview errors %>
            <%
              s_name = @practice.name
              tagline = @practice.tagline
              initiating_facility = @practice.has_facility?
              initiating_facility_type = @practice.initiating_facility_type
              summary = @practice.summary
              date_initiated = @practice.date_initiated
              adoptions = @practice.diffusion_histories
              overview_problem = @practice.overview_problem
              overview_solution = @practice.overview_solution
              overview_results = @practice.overview_results
            %>
            <% if s_name.blank? || tagline.blank? || initiating_facility_type.blank? || initiating_facility.blank? || date_initiated.blank? || summary.blank? %>
              <p class="text-bold">Introduction</p>
              <ul class="dm-publish-errors-ul">
                <% if s_name.blank? %>
                  <li>You must include a name for your innovation</li>
                <% end %>
                <% if tagline.blank? %>
                  <li>You must include a tagline for your innovation</li>
                <% end %>
                <% if date_initiated.blank? %>
                  <li>You must include the initiation date for your innovation</li>
                <% end %>
                <% if initiating_facility_type.blank? || initiating_facility.blank? %>
                  <li>You must include the initiating facility for your innovation</li>
                <% end %>
                <% if summary.blank? %>
                  <li>You must include an innovation summary</li>
                <% end %>
              </ul>
            <% end %>

            <% if overview_problem.blank? || overview_solution.blank? || overview_results.blank? %>
              <p class="text-bold">Overview</p>
              <ul class="dm-publish-errors-ul">
                <% if overview_problem.blank? %>
                  <li>You must include a problem statement for your innovation</li>
                <% end %>
                <% if overview_solution.blank? %>
                  <li>You must include a solution statement for your innovation</li>
                <% end %>
                <% if overview_results.blank? %>
                  <li>You must include a results statement for your innovation</li>
                <% end %>
              </ul>
            <% end %>


            <% if adoptions.blank? %>
              <p class="text-bold">Adoptions</p>
              <ul class="dm-publish-errors-ul">
                <li>You must include at least one adoption</li>
              </ul>
            <% end %>

            <%# Contact error %>
            <%
              email = @practice.support_network_email
            %>
            <% if email.blank? %>
              <p class="text-bold">Contact</p>
              <ul class="dm-publish-errors-ul margin-bottom-0">
                <li>You must include a support network email</li>
              </ul>
            <% end %>
          <% end %>
        </div>
      </div>
      <div class="padding-2 text-right">
        <button class="usa-button margin-0 padding-2 back-to-editor-button">Return to editor</button>
      </div>
    </div>
  </div>
</div>