<div id="visn-va-facility-<%= va_facility.id %>-marker-modal" class="font-sans-sm padding-2">
  <%= link_to "#{facility_name_with_common_name(va_facility.official_station_name, va_facility.common_name)}", va_facility_path(va_facility), target: '_blank', class: 'visn-va-facility-marker-modal-link display-block text-bold margin-bottom-2' %>
  <%
    practices_created_count = Practice.get_facility_created_practices(va_facility.station_number).count
    practices_adopted_count = Practice.get_facility_adopted_practices(va_facility.station_number).count
  %>
  <p class="margin-bottom-2 visn-va-facility-modal-practices-created-count"><%= practices_created_count %> practice<%= 's' if practices_created_count != 1 %> created here</p>
  <p class="visn-va-facility-modal-practices-adopted-count"><%= practices_adopted_count %> practice<%= 's' if practices_adopted_count != 1 %> adopted here</p>
</div>