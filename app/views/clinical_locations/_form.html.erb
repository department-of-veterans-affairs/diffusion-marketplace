<%= form_with(model: clinical_location, local: true) do |form| %>
  <% if clinical_location.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(clinical_location.errors.count, "error") %> prohibited this clinical_location from being saved:</h2>

      <ul>
      <% clinical_location.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :short_name %>
    <%= form.text_field :short_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :position %>
    <%= form.number_field :position %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
