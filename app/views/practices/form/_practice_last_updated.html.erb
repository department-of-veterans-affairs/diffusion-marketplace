<% last_updated = PracticeEditorSession.practice_last_updated(@practice.id) %>
<% unless last_updated.blank? %>
  <div class="margin-y-2">
    <div class="practice-last-updated-text">
      <%= last_updated %>
    </div>
  </div>
<% end %>